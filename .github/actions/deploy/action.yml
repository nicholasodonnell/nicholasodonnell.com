name: 'Deploy'
author: 'nicholas@nicholasodonnell.com'
description: 'Deploy'

branding:
  icon: 'globe'
  color: 'green'

inputs:
  github-token:
    description: 'GitHub token'
    required: true

runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.github-token }}
